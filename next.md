## etapa finalizada

* implementação do `MilkProductionRecordRepository`

* método `save`
* método `get_by_id`
* correção das validações do `MilkProductionRecord`
* ajuste para que os atributos recebam corretamente os valores retornados pelas validações
* correção da conversão entre `datetime` e `isoformat`
* ajustes nos testes do model
* todos os testes passando (36/36)

## próximo passo

Continuar a implementação do `MilkProductionRecordRepository` adicionando os métodos:

* `update`
* `delete`

Após concluir o repositório, escrever e validar todos os testes correspondentes para manter a cobertura e garantir o funcionamento completo do CRUD.
